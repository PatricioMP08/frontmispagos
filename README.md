# MiGasto - Frontend (Next.js)
Minimal Next.js frontend pointing to the example backend URL.

## Run locally
1. cd frontend
2. npm install
3. npm run dev
4. Open http://localhost:3000

## Change backend URL
Edit `lib/api.js` and set `BASE_URL` to your deployed backend (Render/Railway).
